# Claude Code — personal overrides (never commit these)
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# config.js holds the Supabase URL + publishable (anon) key.
# Intentionally committed: the anon key is public by design (RLS protects the
# data), and the deployed GitHub Pages site needs it to reach the live backend.
# NEVER put the service_role / secret key in config.js.
